---
description: バランス調整。src/data の数値だけを触る
---

ゲームバランスを調整する。

## 触ってよい範囲

`src/data/` の中だけ。ロジック（`domain/` `application/`）には手を入れない。

計算式そのものを変える必要があると判断した場合は、勝手に変えずに理由を説明して私の判断を仰ぐ。

## 手順

1. 何が問題か（強すぎる / 弱すぎる / 単調）を具体的に言語化する。
2. `src/data/` の該当する数値を変える。
3. `npm run check` を通し、開発サーバで実際にプレイして確認する。
4. 変更した数値と、変更前後で何が変わったかを報告する。

$ARGUMENTS

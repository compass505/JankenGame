# じゃんけんRPG

じゃんけんの要素を戦闘に組み込んだ小規模RPG。ブラウザで動く。

## 技術スタック

TypeScript / Vite / Vitest。フレームワークは使わず、素の DOM で組む。

## いま何をしているか

現在のフェーズと決定事項は必ず `docs/` を読んで把握すること。**`docs/` がこのプロジェクトの正**であり、
会話の履歴ではない。設計と食い違う実装を見つけたら、実装ではなく先に `docs/` を疑う。

| ファイル | 内容 |
| --- | --- |
| `docs/00_research.md` | 調査 |
| `docs/01_requirements.md` | 要件定義 |
| `docs/02_architecture.md` | 基本設計 |
| `docs/03_detailed-design.md` | 詳細設計 |
| `docs/04_test-plan.md` | テスト計画 |
| `docs/05_assets.md` | 画像素材の発注仕様 |
| `docs/adr/` | 覆せない決定と、その理由 |

**戦闘方式（じゃんけんをどう組み込むか）は未確定。** `docs/adr/0001-battle-model.md` で確定させるまで、
`domain/battle.ts` の中身を勝手に決め打ちで書かない。

## レイヤ規約

依存の向きは常に `ui → application → domain` の一方向。逆流させない。

| ディレクトリ | 責務 | 禁止事項 |
| --- | --- | --- |
| `src/domain/` | 純粋ロジック。状態を受け取り、新しい状態を返す | DOM・`Math.random()`・`Date.now()`・I/O 一切禁止 |
| `src/application/` | ゲーム進行。domain を繋ぐ | DOM を直接触らない |
| `src/data/` | 敵・スキル・ステージの数値定義 | ロジックを書かない。データだけ |
| `src/ui/` | 描画と入力 | ここ以外で DOM を触らない |
| `src/lib/` | 汎用ユーティリティ | ゲーム固有の知識を持ち込まない |

### なぜこう切るか

1. **`domain/` を純粋に保つ理由** — 戦闘方式が未確定だから。三すくみ属性・同時出し読み合い・
   行動権奪取のどれに決まっても、`domain/` の中身の差し替えだけで済み、UI に波及しない。
2. **乱数は必ず `src/lib/rng.ts` 経由** — `Math.random()` を直接呼ぶとテストが再現不能になる。
   シードを渡せば同じ試合が必ず再現される状態を壊さない。
3. **`data/` を分離する理由** — バランス調整はリリース後もずっと続く。数値がデータファイルに
   集まっていれば、ロジックを触らずに回せる。

## コマンド

```bash
npm run dev        # 開発サーバ (http://localhost:5173)
npm run check      # 型チェック + テスト（これが通らないものは未完成）
npm test           # テストのみ
npm run typecheck  # 型チェックのみ
```

## サブエージェント

サブエージェントは**空のコンテキストで起動する**。会話の経緯を引き継げないため、
「ここまでの流れを踏まえた判断」には向かない。**独立して検証できる仕事にだけ使う。**
迷ったら使わず、自分でやること。

**手を動かすのは安いモデル、判断するのは Opus** という分担で進める。

| 工程 | 担当 | なぜ |
| --- | --- | --- |
| 調査（`/research`） | `battle-research` (sonnet) | 案出しは発散の作業。対話履歴がなくても成立し、成果物が1ファイルに収まる |
| 要件定義・設計（`/spec` `/design`） | **メイン (Opus)** | ユーザーとの対話で決まる。ここの手戻りが最も高くつくため削らない |
| 実装（`/impl`） | **Codex (gpt-5.6-luna)** | 設計が確定していれば手を動かす作業。規約は `AGENTS.md` が持つ |
| エラー時の判断 | **メイン (Opus)** | 2回同じ箇所で落ちたら Codex に差し戻さず自分で直す |
| 設計適合レビュー（`/conform`） | `design-conformance` (haiku) | 検査対象が絞られ、出力形式も固定。判断の余地が小さい |
| 素材の洗い出し（`/assets`） | `asset-brief` (sonnet) | プロンプト文の質が成果物そのもの。ここを削ると画風がぶれる |
| 行き詰まったとき | `codex-rescue` (Codex) | 別系統のモデルの視点。同じモデルで考え直しても同じ結論になりやすい |

サブエージェントの報告は**鵜呑みにしない**。指摘は自分でコードを読んで裏を取ってから採否を決める。
sonnet は実装コストを楽観的に見積もる傾向があるので、調査結果の「3日で作れる」は自分で検証する。

### Codex に渡す前に必ずコミットする

`codex exec` はワークツリーを直接書き換える。渡す前にコミットしておけば、
`git diff` が Codex の変更そのものになり、まずければ `git checkout .` で戻せる。

規約は `AGENTS.md` が持っているので、**プロンプトに規約を書き写さない。**
設計のどの部分を実装するのかだけを指定する。

### メインで行うもの

- **要件定義・設計** — 対話を引き継げないサブエージェントには渡せない
- **小さな修正** — コールドスタート（CLAUDE.md と docs の読み直し）のほうが高い
- **Codex の差分の確認** — `npm run check` が通っても設計と違うことはある

### 検査はまずスクリプトで

`scripts/check-layers.sh` で判定できる規約違反に、モデルのトークンを使わない。
新しい規約を足すときは、**まず grep で書けないかを検討する**。書けるならスクリプトに追加し、
`design-conformance` には書かない。

## 画像素材

画像は imagegen で生成する。**Claude 側では生成できない。**

1. `docs/05_assets.md` に発注仕様（ファイル名・サイズ・プロンプト）を用意する
2. ユーザーに提示する。ユーザーが Codex 経由で imagegen に依頼して画像を作る
3. ユーザーが `public/assets/` に指定のファイル名で置く
4. Claude 側で読み込んで表示を確認する

**素材待ちで実装を止めない。** CSS の図形や絵文字でプレースホルダを置き、
後から差し替えられる形で先に組み上げる。ファイル名は仕様書で確定したものを使い、後から変えない。

## 作業のしかた

- `.ts` を編集すると hook が型チェックと関連テストを自動実行する。失敗したら**その場で直す**。
  次の作業に進まない。
- 表示や操作の確認は、開発サーバを起動してブラウザで自分で確認する。ユーザーに確認を代行させない。
- テストは `tests/unit/` に純粋ロジック、`tests/scenario/` に戦闘まるごとの再現テストを置く。
- 数値のバランス調整は `src/data/` の中だけで完結させる。ロジック側に定数を散らさない。

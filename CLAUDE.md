# じゃんけんRPG

じゃんけんを中核に据えた小規模RPG。ブラウザで動く。3日で作り切る。

**戦闘方式は確定済み**（`docs/adr/0001-battle-model.md`）。
**同じ手の連打は「熱」で罰する**（`docs/adr/0002-hand-heat.md`）。グリコ（じゃんけん階段）の
「**勝ち方によって得られるものが違う**」非対称を戦闘に持ち込む。行動はじゃんけん1回だけ。
グーは「にらみ」を溜めて爆発する手、チョキは高火力、パーは削られてから効く手。
**ADR に書いた決定と却下理由は覆さない。**

## 技術スタック

TypeScript / Vite / Vitest。フレームワークは使わず、素の DOM で組む。

## ドキュメントを読む

**`docs/` がこのプロジェクトの正**であり、会話の履歴ではない。
設計と食い違う実装を見つけたら、実装ではなく先に `docs/` を疑う。
現在のフェーズと決定事項は必ず `docs/` を読んで把握すること。

| ファイル | 内容 | いつ読むか |
| --- | --- | --- |
| `docs/00_research.md` | 調査（グリコの非対称・期待値・進行方式） | 仕様や数値を決めるとき |
| `docs/00b_research-battle.md` | 旧調査（戦闘へのダメージ変換） | 戦闘方式を比べるとき |
| `docs/01_requirements.md` | 要件定義 | 常に |
| `docs/02_architecture.md` | 基本設計 | 実装前 |
| `docs/03_detailed-design.md` | 詳細設計 | **実装・テスト作成のたび** |
| `docs/04_test-plan.md` | テスト計画 | テストを書くとき |
| `docs/05_assets.md` | 画像素材の発注仕様と、不要と判断したもの | 素材を扱うとき |
| `docs/05_assets-order.md` | 上の**作業用**。作るものとプロンプトだけ | 画像を生成させるとき |
| `docs/adr/` | 覆せない決定と、その理由 | 決定を蒸し返しそうなとき |
| **`docs/10_workflow.md`** | **工程・コミット・テスト・素材の手順** | **工程に入るとき、コミットするとき** |
| **`docs/11_delegation.md`** | **サブエージェント / Codex の運用** | **他のモデルに何かを渡す前に必ず** |

CLAUDE.md には常時効く規約だけを置く。**手順の詳細は上の2つに切り出してある。**
渡す前・コミット前に読むこと。読まずに進めると、一度潰した失敗を繰り返す。

## レイヤ規約

依存の向きは常に `ui → application → domain` の一方向。逆流させない。

| ディレクトリ | 責務 | 禁止事項 |
| --- | --- | --- |
| `src/domain/` | 純粋ロジック。状態を受け取り、新しい状態を返す | DOM・`Math.random()`・`Date.now()`・I/O 一切禁止 |
| `src/application/` | ゲーム進行。domain を繋ぐ | DOM を直接触らない |
| `src/data/` | 敵・スキル・ステージの数値定義 | ロジックを書かない。データだけ |
| `src/ui/` | 描画と入力 | ここ以外で DOM を触らない |
| `src/lib/` | 汎用ユーティリティ | ゲーム固有の知識を持ち込まない |

### なぜこう切るか

1. **`domain/` が `data/` を import しない理由** — 数値をすべて引数で受け取れば、
   `/balance` で `src/data/` を書き換えても `domain/` のテストが1本も壊れない。
   バランス調整はリリース後もずっと続くので、ここを構造で守る。
2. **乱数は必ず `src/lib/rng.ts` 経由** — `Math.random()` を直接呼ぶとテストが再現不能になる。
   シードを渡せば同じ試合が必ず再現される状態を壊さない。
3. **`data/` を分離する理由** — バランス調整はリリース後もずっと続く。数値がデータファイルに
   集まっていれば、ロジックを触らずに回せる。

**`domain/` は `src/data/` を import しない。** 手の値も敵の定義も引数で受け取る。

## 絶対に守ること

- **`npm run check` が通らない状態でコミットしない。** 壊れた状態はセーブポイントにならない。
  唯一の例外は `/test` 直後（実装がまだ無く落ちるのが正しい）。条件は `docs/10_workflow.md`。
- **コミットしたら毎回 push する。** 手元にだけ溜めない。
- **`Math.random()` を直接呼ばない。** 乱数は `src/lib/rng.ts` 経由。
- **`.ts` の編集で hook が失敗したら、その場で直す。** 次の作業に進まない。
- **リポジトリは Public。** README とコミットメッセージは他人が読む前提で書く。
  https://github.com/compass505/JankenGame

## コマンド

```bash
npm run dev           # 開発サーバ（5173 が埋まっていれば PORT で別ポートに逃がせる）
npm run check         # 型 + テスト + レイヤ規約。これが通らないものは未完成
npm test              # テストのみ
npm run typecheck     # 型チェックのみ
npm run check:layers  # レイヤ規約のみ（scripts/check-layers.sh）
```

### 環境の注意点

- **TypeScript は 7 系**。`baseUrl` は削除されているので使わない。
  `paths` は `"@/*": ["./src/*"]` のように `./` 始まりで書く。
- **Vitest の設定は `vite.config.ts` に同居**。`defineConfig` は `vitest/config` から import する
  （`vite` から import すると `test` キーが型エラーになる）。
- **hook は Codex の書き込みには効かない。** 詳細は `docs/11_delegation.md`。

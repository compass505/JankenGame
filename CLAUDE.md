# じゃんけんRPG

じゃんけんの要素を戦闘に組み込んだ小規模RPG。ブラウザで動く。

## 技術スタック

TypeScript / Vite / Vitest。フレームワークは使わず、素の DOM で組む。

## 進め方

**3日で作り切る規模**。工程はこの順に進め、飛ばさない。

```
/research → /spec → /design → /test → /impl → /conform → /balance
  調査      要件定義   設計    テスト作成  実装    適合確認   調整
```

| | 工程 | 成果物 |
| --- | --- | --- |
| 0日目 | ハーネス整備 | **完了済み** |
| 1日目 | 調査 → 要件定義 → 基本設計 | `docs/00〜02`, ADR |
| 2日目 | 詳細設計 → テスト作成 → 実装 | `docs/03`, `tests/`, `src/` |
| 3日目 | 適合確認 → バランス調整 | `src/data/` |

### 3日で目指さないもの

**バランスの完璧さは目標ではない。** ユーザーの方針として、動くものを3日で作り、
バランスは後日ゆっくり良くしていく。だから数値は `src/data/` に集約してある。
3日目に調整が終わらなくても、それは失敗ではない。

削る判断に迷ったら、**バランスの作り込みから削る。** 遊べる状態まで到達することを優先する。

## リポジトリ

- **GitHub: https://github.com/compass505/JankenGame（Public）**
- **人に見せる目的で公開している。** README とコミットメッセージは他人が読む前提で書く。

### コミットと push

**コミットのタイミングは Claude が判断してよい**（ユーザーから委任済み）。
コミットしたら**毎回 push する**。手元にだけ溜めない。

| タイミング | 例 |
| --- | --- |
| 工程の成果物が固まったとき | 調査完了、要件確定、設計完了 |
| モジュール1つが動いてテストも通ったとき | `domain/janken.ts` + そのテスト |
| **Codex に渡す直前** | 差分を切り分けるため（後述） |

**`npm run check` が通らない状態でコミットしない。** 壊れた状態はセーブポイントにならない。

### GitHub Pages

実装が動いてから設定する。今はデプロイするものがないので不要。
**2日目に実装が動いた段階で、ユーザーに提案すること。** 遊べるURLがあると人に見せやすい。

## いま何をしているか

現在のフェーズと決定事項は必ず `docs/` を読んで把握すること。**`docs/` がこのプロジェクトの正**であり、
会話の履歴ではない。設計と食い違う実装を見つけたら、実装ではなく先に `docs/` を疑う。

| ファイル | 内容 |
| --- | --- |
| `docs/00_research.md` | 調査 |
| `docs/01_requirements.md` | 要件定義 |
| `docs/02_architecture.md` | 基本設計 |
| `docs/03_detailed-design.md` | 詳細設計 |
| `docs/04_test-plan.md` | テスト計画 |
| `docs/05_assets.md` | 画像素材の発注仕様 |
| `docs/adr/` | 覆せない決定と、その理由 |

**戦闘方式（じゃんけんをどう組み込むか）は未確定。** `docs/adr/0001-battle-model.md` で確定させるまで、
`domain/battle.ts` の中身を勝手に決め打ちで書かない。

## レイヤ規約

依存の向きは常に `ui → application → domain` の一方向。逆流させない。

| ディレクトリ | 責務 | 禁止事項 |
| --- | --- | --- |
| `src/domain/` | 純粋ロジック。状態を受け取り、新しい状態を返す | DOM・`Math.random()`・`Date.now()`・I/O 一切禁止 |
| `src/application/` | ゲーム進行。domain を繋ぐ | DOM を直接触らない |
| `src/data/` | 敵・スキル・ステージの数値定義 | ロジックを書かない。データだけ |
| `src/ui/` | 描画と入力 | ここ以外で DOM を触らない |
| `src/lib/` | 汎用ユーティリティ | ゲーム固有の知識を持ち込まない |

### なぜこう切るか

1. **`domain/` を純粋に保つ理由** — 戦闘方式が未確定だから。三すくみ属性・同時出し読み合い・
   行動権奪取のどれに決まっても、`domain/` の中身の差し替えだけで済み、UI に波及しない。
2. **乱数は必ず `src/lib/rng.ts` 経由** — `Math.random()` を直接呼ぶとテストが再現不能になる。
   シードを渡せば同じ試合が必ず再現される状態を壊さない。
3. **`data/` を分離する理由** — バランス調整はリリース後もずっと続く。数値がデータファイルに
   集まっていれば、ロジックを触らずに回せる。

## コマンド

```bash
npm run dev           # 開発サーバ（5173 が埋まっていれば PORT で別ポートに逃がせる）
npm run check         # 型 + テスト + レイヤ規約。これが通らないものは未完成
npm test              # テストのみ
npm run typecheck     # 型チェックのみ
npm run check:layers  # レイヤ規約のみ（scripts/check-layers.sh）
```

### 環境の注意点

- **TypeScript は 7 系**。`baseUrl` は削除されているので使わない。
  `paths` は `"@/*": ["./src/*"]` のように `./` 始まりで書く。
- **Vitest の設定は `vite.config.ts` に同居**。`defineConfig` は `vitest/config` から import する
  （`vite` から import すると `test` キーが型エラーになる）。

## サブエージェント

サブエージェントは**空のコンテキストで起動する**。会話の経緯を引き継げないため、
「ここまでの流れを踏まえた判断」には向かない。**独立して検証できる仕事にだけ使う。**
迷ったら使わず、自分でやること。

分担の軸は1つ。**発散・展開・検査は安いモデル、収束・決定は Opus。**

| 工程 | 安いモデルに出す部分 | Opus が持つ部分 |
| --- | --- | --- |
| 調査 `/research` | 案の洗い出し — `battle-research` (sonnet) | 実装コストの検証、ユーザーへの提示 |
| 要件定義 `/spec` | 抜け漏れの検査 — `spec-review` (haiku) | **案の選択、ADR、「やらないこと」の線引き** |
| 基本・詳細設計 `/design` | **なし（出さない）** | **全部** |
| テスト作成 `/test` | テストの記述 — `test-designer` (sonnet) | 期待値の根拠が設計にあるかの確認 |
| 実装 `/impl` | 実装 — **Codex (gpt-5.6-luna)** | 差分の確認、**エラー時の切り分け** |
| レビュー `/conform` | 適合検査 — `design-conformance` (haiku) | 指摘の採否 |
| 素材 `/assets` | 発注仕様 — `asset-brief` (sonnet) | 点数を絞る判断 |
| バランス `/balance` | 数値の書き換え — Codex (luna) | 何が問題かの言語化、プレイしての確認 |
| 行き詰まったとき | `codex-rescue` (Codex) | 別系統のモデルの視点 |

### 設計だけ委譲しないのは意図的

`/design` は手戻りが最も高く、かつ **`/test` と `/impl` がこの精度に依存している**。
ここが曖昧なまま下流に流れると、曖昧さが増幅されて返ってくる。
浮かせたトークンは手戻り1回で吹き飛ぶ。

### テストの層と担当

テストは3層あり、**層によって失敗時の担当が違う**。実行自体はどの層もトークン 0。

| 層 | 置き場所 | 検証するもの | 実行 |
| --- | --- | --- | --- |
| ユニット | `tests/unit/` | `domain/` の純粋関数 | vitest |
| **シナリオ（結合の本体）** | `tests/scenario/` | `domain` + `application` を通した戦闘1試合 | vitest |
| ブラウザ確認 | なし（手動） | UI を含めた画面遷移・表示・操作 | 開発サーバ |

**結合テストの本体は `tests/scenario/`。** UI を含まないので自動化でき、シードを固定すれば
決定的に回る。ここに投資するのが最も安い。ブラウザでしか確認できないものだけを手動に回す。

### 失敗したときの担当

**修正の手を動かすのは luna。Opus は「原因箇所が示されていないとき」だけ先に立つ。**

分岐は層ではなく、**失敗が原因箇所を示しているか**で決める。

| 失敗の出かた | 1回目 | 2回目 |
| --- | --- | --- |
| 型エラー・レイヤ規約違反（ファイルと行が出る） | **luna** | **Opus** |
| 例外・スタックトレースが実装のファイルを指している | **luna** | **Opus** |
| **アサーション失敗**（期待値と実測値だけ。原因箇所は不明） | **Opus が見立ててから luna** | **Opus** |
| **ハング・タイムアウト**（無限ループ。どこで回っているか不明） | **Opus が見立ててから luna** | **Opus** |

「Opus が見立てる」は深い分析ではない。**どのファイルが怪しいかを絞って luna に名指しで渡す**だけ。
ここに時間をかけない。

### luna に渡すときは触れる範囲を1つに絞る

原因箇所が不明なまま渡すと、luna は目についた箇所を場当たり的に直し、別の層を壊す。
これを防ぐのは「委譲しないこと」ではなく、**触れるファイルを指定すること**。

```bash
codex exec --model gpt-5.6-luna --full-auto "tests/scenario/battle.test.ts が失敗している。
src/domain/battle.ts だけを修正して通す。他のファイルとテストは変更しない。" < /dev/null
```

**渡す前にコミットしておけば、外れても `git checkout .` で無料で戻せる。**
安いモデルに先に試させるほうが期待値が高い。

2回で切り上げるのは、原因が設計側にあるとき Codex は何度やっても同じ壁に当たるため。

### テストは実装より先に、別のモデルで書く

実装と同じモデルにテストを書かせると、テストは「仕様通りか」ではなく
「実装した通りか」を検証してしまい、バグを追認する。

`/test` → `/impl` の順を崩さない。先にテストがあることで、Codex の仕事が
「既にあるテストを通す」という機械的な作業になり、成功率も上がる。
**Codex がテストを書き換えていないかは毎回 `git diff tests/` で確認する。**

サブエージェントの報告は**鵜呑みにしない**。指摘は自分でコードを読んで裏を取ってから採否を決める。
sonnet は実装コストを楽観的に見積もる傾向があるので、調査結果の「3日で作れる」は自分で検証する。

### Codex に渡す前に必ずコミットする

`codex exec` はワークツリーを直接書き換える。渡す前にコミットしておけば、
`git diff` が Codex の変更そのものになり、まずければ `git checkout .` で戻せる。

規約は `AGENTS.md` が持っているので、**プロンプトに規約を書き写さない。**
設計のどの部分を実装するのかだけを指定する。

```bash
codex exec --model gpt-5.6-luna --full-auto "<設計のどの部分を実装するか>" < /dev/null
```

**`< /dev/null` は必須。** 付けないと Codex が stdin からの追加入力を待って止まる。

### メインで行うもの

- **要件定義・設計** — 対話を引き継げないサブエージェントには渡せない
- **小さな修正** — コールドスタート（CLAUDE.md と docs の読み直し）のほうが高い
- **Codex の差分の確認** — `npm run check` が通っても設計と違うことはある

### 検査はまずスクリプトで

`scripts/check-layers.sh` で判定できる規約違反に、モデルのトークンを使わない。
新しい規約を足すときは、**まず grep で書けないかを検討する**。書けるならスクリプトに追加し、
`design-conformance` には書かない。

## 画像素材

画像は imagegen で生成する。**Claude 側では生成できない。**

1. `docs/05_assets.md` に発注仕様（ファイル名・サイズ・プロンプト）を用意する
2. ユーザーに提示する。ユーザーが Codex 経由で imagegen に依頼して画像を作る
3. ユーザーが `public/assets/` に指定のファイル名で置く
4. Claude 側で読み込んで表示を確認する

**素材待ちで実装を止めない。** CSS の図形や絵文字でプレースホルダを置き、
後から差し替えられる形で先に組み上げる。ファイル名は仕様書で確定したものを使い、後から変えない。

## 作業のしかた

- `.ts` を Edit / Write すると hook が型チェック・関連テスト・レイヤ規約を自動実行する。
  失敗したら**その場で直す**。次の作業に進まない。
- テストは `tests/unit/` に純粋ロジック、`tests/scenario/` に戦闘まるごとの再現テストを置く。
- 数値のバランス調整は `src/data/` の中だけで完結させる。ロジック側に定数を散らさない。

### hook は Codex の書き込みには効かない

hook は Claude の Edit / Write ツールに紐づいている。`codex exec` は別プロセスとして
ファイルを直接書くため、**hook は発火しない。**

したがって Codex から戻ってきたら、**必ず手動で回す。**

```bash
git diff --stat && npm run check && git diff tests/
```

自動化に頼れない唯一の箇所なので、手順として固定する。

### ブラウザでの確認

表示や操作の確認は、開発サーバを起動して**自分で確認する。ユーザーに確認を代行させない。**
`npm run check` が通っても、表示崩れ・操作感・面白さは保証されない。

これは現状メイン（Opus）が担当する。スクリーンショットを扱うためメイン文脈を最も消費するが、
画面数が少ないうちは分離のコールドスタートが上回る。
**スクリーンショットが嵩んできたら、表示崩れの検出だけをサブエージェントに分ける**ことを検討する
（「面白いか」は判断なので分けられない）。

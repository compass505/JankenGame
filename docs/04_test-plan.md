# テスト計画

> ステータス: **一部着手** — `lib/rng` のみ実装済み

## 方針

| 種別 | 置き場所 | 対象 |
| --- | --- | --- |
| ユニット | `tests/unit/` | `domain/` の純粋関数、`lib/` |
| シナリオ | `tests/scenario/` | 戦闘まるごと。シードを固定して結果を再現する |

UI はテストしない。3日の規模ではコストが見合わないため、ブラウザでの目視確認に任せる。

## テスト済み

- `lib/rng` — 再現性、値域、`pick` の境界

## これから書く

<!-- /design 完了後に列挙する -->

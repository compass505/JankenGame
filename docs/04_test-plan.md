# テスト計画

> ステータス: **一部着手** — `lib/rng` のみ実装済み

## 3層の方針

| 層 | 置き場所 | 検証するもの | 自動化 |
| --- | --- | --- | --- |
| ユニット | `tests/unit/` | `domain/` の純粋関数、`lib/`、**`application/` の配線のうちシナリオでは見えないもの** | vitest |
| **シナリオ** | `tests/scenario/` | `domain` + `application` を通した戦闘1試合 | vitest |
| ブラウザ確認 | 手動 | UI を含めた画面遷移・表示・操作 | なし |

## 結合テストの範囲

**3日で作る規模なので、しっかりした結合テストは行わない。** そのぶん、
自動化できる範囲を `tests/scenario/` に寄せる。UI を含めない結合であれば
vitest で決定的に回せるため、コストがほぼかからない。

### `tests/scenario/` は3本まで

1. プレイヤーが勝つまでの1試合 — 決着時の状態
2. プレイヤーが負けるまでの1試合 — HP が負にならない、敗北状態になる
3. あいこが連続しても進行する — **無限ループにならない**（ターン上限で検出）

### バランス調整に耐えること

シナリオテストは**決着時の状態だけ**を検証する。途中の数値を検証すると、
`src/data/` の数値を変えるたびにテストが壊れ、バランス調整ができなくなる。

- 書く: 「決着したとき、勝者の HP が 1 以上」
- 書かない: 「3ターン目に HP が 12」

### ブラウザ確認は手動、回数を絞る

E2E フレームワークは導入しない（3日の規模に対して重い）。
次の3回だけ、開発サーバを起動して手で確認する。

1. 各画面が組み上がった時点
2. 全画面が繋がった時点
3. バランス調整の最終確認

## UI はユニットテストしない

`src/ui/` にテストは書かない。3日の規模ではコストが見合わないため、
ブラウザでの目視確認に任せる。そのぶん `domain` を厚く検証する。

## テスト済み

- `lib/rng` — 再現性、値域、`pick` の境界

## 書いたもの（実装待ち）

**`/test` で作成済み。`/impl` の前なので、モジュールが無くて落ちるのが正しい状態。**
設計は `docs/03_detailed-design.md`。**期待値の根拠はすべてそこにある。**

| ファイル | 行数 | 中身 |
| --- | --- | --- |
| `tests/unit/hand.test.ts` | 75 | `judge` の9通り、`HANDS` の順序、対称性、勝敗の分布 |
| `tests/unit/handTable.test.ts` | 263 | 強化の上限、`buildHandTable` が damage にだけ加算すること、**熱**（下の節） |
| `tests/unit/enemy.test.ts` | 225 | フェーズ境界、確率の和と下限、`rng` の呼び出し回数 |
| `tests/unit/battle.test.ts` | 497 | **本体**。手順0、具体例1〜5、あいこ、定数、不変条件1〜8、再現性 |
| `tests/unit/game.test.ts` | 182 | **熱の配線のみ**（`application/game.ts`）。下の節 |
| `tests/scenario/game.test.ts` | 167 | シナリオ3本 |

### テストの前提として置いたこと（実装側が守るもの）

- **敵の手を固定する方法**: 重みを1つの手に集中させ（`weight = 1`、他 0）、
  `rng.next()` が `0.5` を返すようにする。`UNIFORM_MIX = 0.3` なので集中させた手の
  確率は 0.8 になり、必ずその手が選ばれる。**この性質は `docs/03` 節3 の式から導ける**
- **`resolveTurn` が決着済み state を返すときの参照同一性は縛っていない**（`toEqual` で検証）。
  同じ参照を返す実装と、同じ値の新しいオブジェクトを返す実装のどちらでもよい
- **シナリオ1・2 は勝ち／負けのシードを決め打たない。** 300シードを順に試し、
  最初に条件へ合致したものを検証する。見つからなかった場合は
  **「バランスの偏り」だと分かる失敗メッセージ**を出す（実装のバグと切り分けるため）

### 内訳

#### `tests/unit/`

| ファイル | 検証するもの |
| --- | --- |
| `hand.test.ts` | `judge` の9通り。`judge(a,a)==='draw'`、勝敗の対称性 |
| `handTable.test.ts` | `canUpgrade` の上限、`applyUpgrade` が上限で据え置き、`buildHandTable` が **damage にだけ**加算し `heal` を動かさない |
| `enemy.test.ts` | `enemyPhase` の境界（`hp*2 === maxHp` は `desperate`）、`handProbabilities` の**和が1**と**全手 0.1 以上**、`decideEnemyHand` が `rng.next()` を**ちょうど1回**呼ぶ |
| `battle.test.ts` | 節4の不変条件1〜8と、具体例1〜5をそのまま |

**`battle.test.ts` が本体。** 特にこの3つは落とさない。

- **相手が生き残るターンでは `playerHp + enemyHp` が必ず1以上減る**（終了保証）。
  **「毎ターン必ず減る」と書かないこと。** 相手を倒したターンだけは回復ぶんで総和が
  増えうる（節4の不変条件4）。ここを取り違えると、**正しい実装なのにテストが落ちる**
- **耐性0.5のパーで回復がダメージを上回らない**（例3。ここが壊れると戦闘が終わらない）
- **同時に0以下ならプレイヤーの敗北**（例5。引き分けを作らない）

また、**決着済みの `state` に `resolveTurn` を呼んでも `turn` は増えず、同じ state が返る**
（節4の手順0）。不変条件3・7は**未決着の state に対してのみ**成り立つ。

#### `tests/scenario/`

方針どおり**3本まで**。`application/game.ts` を通す。
固定シードで回し、**決着時の状態だけ**を見る。途中の数値は見ない。

1. プレイヤーが勝つまでの1周 — 5体倒して `phase === 'result'` かつ `cleared === true`
2. プレイヤーが負ける1試合 — `cleared === false`、`playerHp === 0`、HPが負になっていない
3. **あいこが連続しても進行する** — ターン上限（例: 200）を置いて回し、
   **上限に達する前に必ず決着すること**。にらみが `STARE_MAX` を超えないこと

### 書かないもの

- `src/ui/` のテスト（方針どおり）
- `src/data/` の数値そのものを検証するテスト（`/balance` で壊れるため）

## 熱の追加ぶん（2026-08-10・`docs/adr/0002-hand-heat.md`）

> ステータス: **書き終えた**（`/test`・2026-08-10）。実装は `/impl` 待ち。

| ファイル | 中身 |
| --- | --- |
| `tests/unit/handTable.test.ts` | **追記**。`applyHeat` / `advanceHeat` / 定数（下の表） |
| `tests/unit/game.test.ts` | **新規**。`application/game.ts` の熱の配線（節5）。下の「配線のテスト」 |

### `domain/handTable.ts` のぶん

`tests/unit/handTable.test.ts` に追記する。**新しいファイルは作らない**（同じモジュールなので）。

| 検証するもの | 根拠 |
| --- | --- |
| `applyHeat(base, NO_HEAT)` が `base` と同じ値 | 節2.5 不変条件 |
| `applyHeat` が `heal` と `stareBonus` を動かさない | 節2.5。**強化と同じく damage にしか触らない** |
| `floor(熱 / HEAT_GAIN)` の段差（熱 0→0段、4→1段、7→**1段**、13→3段） | 節2.5 具体例。**7で2段にならないこと**が要点 |
| 弱化しても `damage` が 1 未満にならない | 節2.5 |
| 弱化量が `HEAT_MAX_PENALTY` で頭打ち（熱40でも3段） | 節2.5 |
| `advanceHeat` が**先に全部1冷ましてから**出した手に加算する | 節2.5。`{scissors:4}` に `'rock'` を出すと `{rock:4, scissors:3}` |
| `advanceHeat` が引数を書き換えず新しいオブジェクトを返す | 節2.5 |

### `application/game.ts` の配線のテスト（`tests/unit/game.test.ts`）

**シナリオテストは決着時の状態しか見ないので、熱の配線は素通りしてしまう。**
「戦闘ごとにリセットし忘れた」「敵側の熱を進め忘れた」は、それでも1周クリアできてしまうため
シナリオ3本のどれも落ちない。熱の実装の大半は `application` 側（`docs/03` 節5）にあるので、
**ここだけを対象にしたユニットテストを1本足した。**

| 検証するもの | 根拠（`docs/03` 節5） |
| --- | --- |
| `startGame` 直後は両方の熱が `NO_HEAT` | 各関数の表・不変条件 |
| `playHand` 後の熱が `advanceHeat(前, 出した手)` と一致（**敵側も**） | 「熱の更新」 |
| **あいこのターンでも熱が進む** | 「あいこのターンも更新する」 |
| `chooseUpgrade` 直後は両方 `NO_HEAT` に戻る | 各関数の表 |
| `heatPenalties` が 0〜`HEAT_MAX_PENALTY` に収まる | 「表示用」の関数表 |
| `playerHandTable` に熱が乗る（`heal`・`stareBonus` は動かない） | 「`ctx` の組み立て」 |

**phase遷移・強化・クリア判定はここでは見ない**（`tests/scenario/` の担当）。重複させない。

**敵の手は `state.lastLog.enemyHand` から取る。** 乱数の実装詳細に期待値を依存させないため。
`playerHandTable` は「前の `damage` から1下がる」形で検証しており、
**`src/data/hands.ts` の数値は1つも焼き込んでいない**（`/balance` で動かしても壊れない）。

### シナリオテストは増やさない

**3本のまま。** ただしシナリオ3（あいこが連続しても進行する）は、
熱で全手が弱ると**1ターンあたりの減りが小さくなる**ため、
**ターン上限に達しないことの意味が重くなる**。既存のテストがそのまま効く。

### `domain/battle.ts` のテストは1本も変えない

熱は `application` が組み立てた `HandTable` として渡るだけで、
`resolveTurn` の契約は変わらない。**`tests/unit/battle.test.ts` に手を入れる必要が出たら、
それは設計から外れた実装をしている合図**なので、テストではなく実装を疑う。

### `src/data/` の数値改定について

グーの `stareBonus` 3・チョキの `damage` 5 は `src/data/hands.ts` の値であり、
**方針どおりテストでは検証しない**（`/balance` で動かせる状態を保つ）。
